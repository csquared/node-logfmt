#! /usr/bin/env node

var logfmt = require('../lib/logfmt'),
    assert = require('assert')

assert.equal(logfmt.parse("foo=bar a=14 baz=\"hello kitty\" cool%story=bro f %^asdf"),
             { "foo": "bar", "a": 14, "baz": "hello kitty", "cool%story": "bro", "f": true, "%^asdf": true })
